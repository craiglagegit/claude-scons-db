name: claude-scons-db
channels:
  - conda-forge
dependencies:
  - python=3.13
  # Runtime dependencies (mirror pyproject.toml [project.dependencies])
  - numpy
  # Dev dependencies (mirror pyproject.toml [project.optional-dependencies.dev])
  - black
  - mypy
  - pytest
  # Interactive development / visualization
  - matplotlib
  - notebook
